const fs = require('fs');
const globals = require('../globals');

// Search the text of all notes for the search term and rank based on relevance. Return a list.

module.exports = function(config, term, callback) {
    return false;
}